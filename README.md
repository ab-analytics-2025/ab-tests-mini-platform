# A/B Tests Mini-Platform

Учебный проект по дисциплине **«Технологии хранения и обработки больших объёмов информации»**.

Цель проекта — создать **мини-платформу** для воспроизводимого анализа A/B-эксперимента:
от загрузки данных до статистических выводов и отчёта с визуализациями **+ UI**.

Датасет: `marketing_AB.csv`.

---

## 📁 Структура проекта

| Папка / файл             | Назначение                                                               |
| ------------------------ | ------------------------------------------------------------------------ |
| **data/**                | Данные проекта                                                           |
| ├─ **raw/**              | Сырые данные                                                             |
| └─ **cleaned/**          | Очищенные данные                                                         |
| **src/**                 | Python-код пайплайна (загрузка, очистка, SRM/balance, статистика, отчёт) |
| **tests/**               | Автотесты (unit + data-quality)                                          |
| **docs/**                | Документация, схемы, ADR, отчёты                                         |
| **notebooks/**           | Jupyter Notebook (EDA/исследования)                                      |
| **reports/**             | Артефакты запуска (report.json, figures/\*.png)                          |
| **backend/**             | API сервис (запуск пайплайна и отдача отчёта/артефактов)                 |
| **frontend/**            | UI (витрина отчёта + графики)                                            |
| **requirements.txt**     | Runtime зависимости Python                                               |
| **requirements-dev.txt** | Dev зависимости (ноуты, тесты, линтеры)                                  |
| **README.md**            | Описание проекта и инструкции                                            |
| **LICENSE**              | Лицензия (MIT)                                                           |

---

## ⚙️ Установка зависимостей

Рекомендуется виртуальное окружение:

```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/Mac:
# source venv/bin/activate
```

### Минимальный набор (для пайплайна)

```bash
pip install -r requirements.txt
```

### Полный набор (разработка + тесты + ноутбуки)

```bash
pip install -r requirements-dev.txt
pip install -e .
```

---

## 🚀 Запуск (локально)

### 1) Python пайплайн (src)

Цель: один запуск → получить cleaned + отчёт + графики в `reports/`.

Интерфейс CLI:

```bash
python -m ab_platform \
  --input data/raw/marketing_AB.csv \
  --cleaned data/cleaned/marketing_AB_clean.csv \
  --report reports/report.json \
  --figures reports/figures
```

### 2) Backend (API)

Backend предоставляет:

- `GET /analyze` — запускает пайплайн и отдаёт JSON-отчёт
- `GET /artifacts/*` — раздаёт PNG графики
- `GET /health` — healthcheck

### 3) Frontend (UI)

Frontend показывает:

- ключевые результаты (SRM/balance, тесты, CI, MDE, recommendation)
- изображения графиков из `/artifacts`

---

## 🧪 Тестирование

Тесты должны включать:

- unit-тесты функций статистики/валидаций
- data-quality тесты (схема/диапазоны/уникальность)

Запуск:

```bash
python -m unittest
```

(или при переходе на pytest)

```bash
pytest -q
```

---

## 👥 Команда проекта и распределение ролей

| №     | Роль                                        | Основные обязанности                                                                                                                 | Участник                                     |
| ----- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------- |
| **1** | **Менеджер проекта**                        | Планирование, ведение доски задач (GitHub Projects), контроль сроков, отчётность преподавателю.                                      | _(ФИО / GitHub-ник)_                         |
| **2** | **Технический лидер (Tech Lead)**           | Определяет структуру проекта, отвечает за качество кода, помогает в ревью и архитектурных решениях.                                  | _(ФИО / GitHub-ник)_                         |
| **3** | **Data Engineer**                           | Загрузка исходных данных, очистка, фильтрация, подготовка финальных выборок.                                                         | _Чернышов Никита Игоревич / kitossy          |
| **4** | **Statistician (аналитик-экспериментатор)** | Проверка дизайна эксперимента (SRM, рандомизация/balance), расчёт тестов (z-тест, bootstrap), доверительные интервалы, MDE/мощность. | _Нелюбин Роман Олегович / Limeryccoga_       |
| **5** | **Data Analyst (визуализация)**             | Анализ и визуализация результатов, построение графиков, интерпретация метрик и итоговых выводов.                                     | _Микехин Никита Вячеславович / FireNike2002_ |
| **6** | **QA / Тестировщик**                        | Автотесты (unit + data-quality), контроль корректности данных и кода.                                                                | _Иванов Максим / Maksim-73_                  |
| **7** | **DevOps / Reproducibility инженер**        | Окружение, Docker, CI (GitHub Actions), контроль воспроизводимости проекта.                                                          | _(ФИО / GitHub-ник)_                         |

---

## 📚 Цель и результаты

К концу проекта должна быть реализована мини-платформа, которая:

- Загружает и очищает реальные данные эксперимента.
- Проверяет корректность дизайна (SRM + баланс распределений признаков).
- Считает метрики и доверительные интервалы.
- Строит визуализации.
- Генерирует отчёт с выводом: **«раскатывать» или «откатывать» эксперимент**.
- Предоставляет UI для просмотра результатов и артефактов.
